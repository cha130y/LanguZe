/**
 * The details the Terms of Use, the Privacy Policy and the contact page share
 * (FR-090, FR-099). They sit here so that one change reaches every page.
 */

/** Abuse reports, suspension appeals, and personal-data requests all arrive here. */
export const CONTACT_EMAIL = 'support@languze.com';

/**
 * Thailand's PDPA requires the data controller to be identifiable, so this has to
 * carry a real name before the pages are public. It is deliberately conspicuous.
 */
export const OPERATOR_NAME = '[OPERATOR NAME]';

/** Shown on each page, so a learner can see which version they are reading. */
export const LAST_UPDATED = '24 กันยายน 2026';
